# Forex Position Size Calculator - Environment Variables Configuration Example

# MetaAPI Credentials (required for real-time data)
METAAPI_API_KEY=your_api_key_here
METAAPI_ACCOUNT_ID=your_account_id_here
METAAPI_REGION=new-york

# Application Settings
NODE_ENV=development
PORT=3000

# Note: Legacy API keys (CurrencyFreaks, FX Rates) have been removed
# This application now uses MetaAPI exclusively for real-time data
# with fallback to manual data entry when API is unavailable
