# Forex Position Size Calculator - Environment Variables Configuration Example

# MetaAPI Credentials (required for real-time data via REST API)
METAAPI_API_KEY=your_api_key_here
METAAPI_ACCOUNT_ID=your_account_id_here
METAAPI_REGION=new-york

# Application Settings
NODE_ENV=development
PORT=3000

# Note: This application uses MetaAPI REST API exclusively for real-time data
# No SDK or WebSocket dependencies required - uses native fetch() for HTTP calls
# Fallback to manual data entry when API is unavailable
# Legacy API support (CurrencyFreaks, FX Rates) has been completely removed
